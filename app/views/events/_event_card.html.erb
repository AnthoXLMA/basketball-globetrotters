<%= link_to event_path(event) do %>
  <div class="card-event">
    <div class="card-event-infos">
      <div>
        <h2><%= event.name %></h2>
        <h2><%= event.location %></h2>
        <h2><%= event.playground_id %></h2>
      </div>
    </div>
  </div>
<% end %>
