<%= link_to event_path(event) do %>
  <div class="card-event">
    <%= image_tag event.banner_url %>
    <div class="card-event-infos">
      <div>
        <h2><%= event.name %></h2>
      </div>
    </div>
  </div>
<% end %>
